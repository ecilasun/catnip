
// GrimR language test file

var cursorX, cursorY;

function DrawRect
{
    using posX, posY;
    using width, height;
    var spanX, spanY;

    spanY = posY;
    while (spanY<spanY+height)
    {
        spanX = posX;
        while (spanX<spanX+width)
        {
            // TODO: plot pixel here at spanX,spanY
            spanX = spanX+1;
        }
        spanY = spanY+1;
    }
}

function Main
{
    DrawRect(512, 384, 128, 96);
}
