// GrimR language test file

var cursorX, cursorY

function DrawRect
begin using width, height
    area = width * height
    using posX,posY
    rightEdge = posX + width
    bottomEdge = posY + height
end
