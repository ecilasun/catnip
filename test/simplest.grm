
// GrimR language test file

var cursorX, cursorY;

function DrawRect
{
    using width, height;
    var area;
    area = width * height;
    using posX, posY;
    rightEdge = posX + width;
    bottomEdge = posY + height;
}

function Main
{
    DrawRect(512,384,128,96);
}
